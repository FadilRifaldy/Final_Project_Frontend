export interface IStore {
    id: string;
    name: string;
    address: string;
    postalCode: string;
    city: string;
    province: string;
    latitude: string;
    longitude: string;
    isActive: boolean;
    opensAt: string;
    closesAt: string;
    createdAt: string;
    updatedAt: string;
}